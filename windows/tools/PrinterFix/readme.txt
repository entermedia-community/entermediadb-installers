Printer Autofix script created by Helge Sverre ( helgesverre.com )

No guarantees of any kind.

Uses IOBit Unlocker to unlock files and delete them
This script deletes registry keys automatically.

Run it as an administrator or it will not work.


To use:

Run RUNME.bat